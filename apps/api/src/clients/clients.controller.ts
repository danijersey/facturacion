import { Body, Controller, Delete, Get, Param, ParseIntPipe, Patch, Post, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { CurrentUser } from '../common/current-user.decorator'; import { JwtAuthGuard } from '../common/jwt-auth.guard'; import { CreateClientDto, UpdateClientDto } from './client.dto'; import { ClientsService } from './clients.service';
@ApiTags('clients') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('clients') export class ClientsController { constructor(private s:ClientsService){} @Get() list(@CurrentUser()u:{id:number}){return this.s.list(u.id)} @Post() create(@CurrentUser()u:{id:number},@Body()d:CreateClientDto){return this.s.create(u.id,d)} @Patch(':id') update(@Param('id',ParseIntPipe)id:number,@CurrentUser()u:{id:number},@Body()d:UpdateClientDto){return this.s.update(id,u.id,d)} @Delete(':id') remove(@Param('id',ParseIntPipe)id:number,@CurrentUser()u:{id:number}){return this.s.remove(id,u.id)} }
