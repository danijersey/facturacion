import { Injectable, NotFoundException } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { Client } from './client.entity'; import { CreateClientDto, UpdateClientDto } from './client.dto';
@Injectable() export class ClientsService { constructor(@InjectRepository(Client) private repo:Repository<Client>){} list(userId:number){return this.repo.find({where:{userId},order:{createdAt:'DESC'}})} async one(id:number,userId:number){const x=await this.repo.findOneBy({id,userId});if(!x)throw new NotFoundException('Client not found');return x} create(userId:number,dto:CreateClientDto){return this.repo.save(this.repo.create({...dto,userId}))} async update(id:number,userId:number,dto:UpdateClientDto){const x=await this.one(id,userId);return this.repo.save(Object.assign(x,dto))} async remove(id:number,userId:number){await this.repo.remove(await this.one(id,userId));return {success:true}} }
