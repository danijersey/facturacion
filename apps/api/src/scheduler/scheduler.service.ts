import{Injectable,Logger}from'@nestjs/common';import{Cron,CronExpression}from'@nestjs/schedule';import{InvoiceStatus}from'../invoices/invoice.entity';import{InvoicesService}from'../invoices/invoices.service';import{MailService}from'../mail/mail.service';@Injectable()export class SchedulerService{private log=new Logger(SchedulerService.name);constructor(private invoices:InvoicesService,private mail:MailService){}@Cron(CronExpression.EVERY_DAY_AT_8AM)async markOverdue(){const items=await this.invoices.overdueCandidates();for(const i of items){i.status=InvoiceStatus.OVERDUE;await this.invoices.save(i);await this.mail.sendReminder(i)}this.log.log(`${items.length} overdue invoices processed`)}}
